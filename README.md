# bash
Various bash scripts


#### 1. Show network applications with established TCP connections

Find all ports using established TCP connections, and get applications that using this ports via `lsof` command.
Finally summarize the connections per application and show them.
